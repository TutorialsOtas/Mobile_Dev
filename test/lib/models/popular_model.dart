import 'package:flutter/material.dart';

class PopularModel extends StatelessWidget {
  const PopularModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
