import 'package:flutter/material.dart';

class DietModel extends StatelessWidget {
  const DietModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
